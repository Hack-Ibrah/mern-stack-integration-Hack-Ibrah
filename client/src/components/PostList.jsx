import React,{useEffect,useState} from 'react'; import axios from 'axios'; import { Link } from 'react-router-dom'; export default function PostList(){ const [posts,setPosts]=useState([]); useEffect(()=>{ axios.get(`${import.meta.env.VITE_API_URL}/posts`).then(res=>setPosts(res.data)); },[]); return (<div><h1>Posts</h1>{posts.map(p=>(<div key={p._id}><Link to={'/post/'+p._id}>{p.title}</Link></div>))}</div>); }